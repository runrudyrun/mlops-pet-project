services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - CONFIG_PATH=configs/params.yaml
      - MODEL_PATH=models/model.pkl
    volumes:
      - ./models:/app/models
      - ./data:/app/data
      - ./reports:/app/reports
